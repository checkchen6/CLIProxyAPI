# Git and GitHub folders
.git
.github

# Docker and CI/CD related files
deploy
.dockerignore
.gitignore
.goreleaser.yml
Dockerfile

# Documentation and license
docs
README.md
README_CN.md
README_JA.md
DEPLOYMENT_CN.md
LICENSE

# Runtime data folders (should be mounted as volumes)
auths
logs
conv
config.yaml
tmp-panel-run*.yaml
tmp-panel-run-auths

# Frontend source tree. The management panel is pre-built and embedded into the
# Go binary at internal/managementasset/embedded/management.html via //go:embed,
# so the web/ sources (and any local node_modules) are neither needed to build
# the image nor referenced by the Go compiler; excluding them keeps the build
# context small.
web

# Development/editor
bin
.vscode
.claude
.codex
.codex-worktrees
.gemini
.serena
.agent
.agents
.antigravitycli
.opencode
.idea
.junie
.worktrees
.bmad
_bmad
_bmad-output
